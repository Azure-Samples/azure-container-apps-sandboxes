# Shared-blob memory swarm (`aca` CLI)

The `aca` CLI variant of the shared-blob-memory swarm. See [`cli/`](cli/) to run it.

The full scenario story (cast table, sequence diagram, customer-value claims,
production tips) and the end-to-end validated Python SDK variant live in
[`python/samples/04-swarms/02-shared-blob-memory`](../../../../python/samples/04-swarms/02-shared-blob-memory).
