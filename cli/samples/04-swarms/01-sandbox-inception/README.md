# Sandbox inception swarm (`aca` CLI)

The `aca` CLI variant of the sandbox-inception swarm. See [`cli/`](cli/) to run it.

The full scenario story (architecture diagram, customer-value claims, production
tips) and the end-to-end validated Python SDK variant live in
[`python/samples/04-swarms/01-sandbox-inception`](../../../../python/samples/04-swarms/01-sandbox-inception).
